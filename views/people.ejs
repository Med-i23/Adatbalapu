<!DOCTYPE html>
<html lang="en">
<%- include('assets/head') %>

<body class="w-100 h-100 d-flex flex-column min-vh-100">
<%- include('assets/header') %>
<div class="container form-container flex-grid">

    <% for (let i = 0; i < users.length; i++){ %>
        <div>
            <div><%= users[i][1] %></div>
            <a href="/addFriend<%= users[i][0] %>&<%= current_id %>"><button>ADD FRIEND</button></a>
        </div>
    <% } %>

</div>
<%- include('assets/footer') %>
</body>
</html>